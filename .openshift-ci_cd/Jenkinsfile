node {
  stage 'Checkout Code'
  checkout scm
  
  stage 'Build'
  openshiftBuild(buildConfig: 'flask-super-app', showBuildLogs: 'true')
  
  stage 'Deploy'
  openshiftDeploy(deploymentConfig: 'flask-demo')
}

